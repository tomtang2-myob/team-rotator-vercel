/**
 * Server Components Index
 * 
 * All server components (without 'use client' directive) are exported from here.
 * These components run on the server and can access backend resources.
 */

// Add server components here as needed
// Example: export { UserList } from './UserList';

